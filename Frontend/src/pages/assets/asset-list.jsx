import { PagePlaceholder } from "@/components/shared/page-placeholder";

export function AssetListPage() {
  return (
    <PagePlaceholder
      eyebrow="Asset registry"
      title="Assets"
      description="Search every tagged asset, inspect its lifecycle state, and understand its current custody at a glance."
      nextStep="F3 adds directory filters, server pagination, registration, and reusable status treatments."
    />
  );
}
