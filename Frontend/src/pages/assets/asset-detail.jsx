import { PagePlaceholder } from "@/components/shared/page-placeholder";

export function AssetDetailPage() {
  return (
    <PagePlaceholder
      eyebrow="Asset passport"
      title="Asset record"
      description="A single traceable record for identity, location, holder, condition, maintenance, and audit history."
      nextStep="F3 adds the Asset Passport signature, legal lifecycle actions, and custody timelines."
    />
  );
}
