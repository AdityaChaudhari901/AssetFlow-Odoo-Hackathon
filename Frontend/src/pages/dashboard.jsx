import { PagePlaceholder } from "@/components/shared/page-placeholder";

export function DashboardPage() {
  return (
    <PagePlaceholder
      eyebrow="Today’s operations"
      title="Dashboard"
      description="See availability, active custody, bookings, maintenance, pending transfers, and overdue returns in one operational view."
      nextStep="F8 connects KPI counters, return queues, quick actions, and recent activity."
    />
  );
}
