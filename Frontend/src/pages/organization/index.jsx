import { PagePlaceholder } from "@/components/shared/page-placeholder";

export function OrganizationPage() {
  return (
    <PagePlaceholder
      eyebrow="Governance"
      title="Organization setup"
      description="Maintain departments, asset categories, employee assignments, and role boundaries from one controlled workspace."
      nextStep="F2 adds departments, categories, and the employee directory with admin-only role changes."
    />
  );
}
