import { PagePlaceholder } from "@/components/shared/page-placeholder";

export function AuditDetailPage() {
  return (
    <PagePlaceholder
      eyebrow="Audit evidence"
      title="Audit cycle"
      description="Compare expected and observed asset state, record discrepancies, and close the cycle with a durable report."
      nextStep="F7 adds per-asset verification, discrepancy reporting, progress, and locked closed-cycle views."
    />
  );
}
