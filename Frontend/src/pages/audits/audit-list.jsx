import { PagePlaceholder } from "@/components/shared/page-placeholder";

export function AuditListPage() {
  return (
    <PagePlaceholder
      eyebrow="Verification cycles"
      title="Asset audits"
      description="Plan scoped inspections, assign auditors, and monitor verification progress without rewriting historical evidence."
      nextStep="F7 adds audit-cycle creation, progress summaries, assigned auditors, and immutable closure."
    />
  );
}
